#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>> t;
    while (t--)
    {
        int c,m;
        cin>> c>>m;
        vector<int> a(c) ;
        vector<int> b(c) ;
        for (int i=0;i<c;i++)
        {
            cin>> a[i] >> b[i];
        }
        
    }

    return 0;
}
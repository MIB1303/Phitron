#include <bits/stdc++.h>
using namespace std;
int main()
{
    int x, y;
    cin >> x >> y;
    if (x != 1 && y != 1)
    {
        cout << 1;
    }
    if (x != 2 && y != 2)
    {
        cout << 2;
    }
    if (x != 3 && y != 3)
    {
        cout << 3;
    }
    return 0;
}